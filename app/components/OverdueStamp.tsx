export function OverdueStamp() {
  return (
    <span
      className="absolute -top-3 -right-3 rotate-[-8deg] bg-alert text-white font-display text-[10px] px-2 py-1 border-2 border-ink shadow-brutal-sm select-none"
      aria-label="Overdue"
    >
      OVERDUE
    </span>
  );
}